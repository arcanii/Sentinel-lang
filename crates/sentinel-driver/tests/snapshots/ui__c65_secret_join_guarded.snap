---
source: crates/sentinel-driver/tests/ui.rs
expression: "reject_stderr(\"c65_secret_join_guarded.sentinel\")"
---
sentinel::mir::secret_leak

  × a `secret` value reaches a conditional branch, which would leak it via
  │ timing
    ╭─[tests/ui/c65_secret_join_guarded.sentinel:27:5]
 26 │     let m: secret bool = xs[if i < len(xs) { i } else { return s }];
 27 │     m && s
    ·     ┬
    ·     ╰── secret value reaches the sink here
 28 │ }
    ╰────
  help: a secret must not influence control flow, a memory address, or a
        variable-latency instruction; restructure to be branch-free (e.g. a
        masked select) or `declassify` the value first if the leak is
        acceptable

sentinel::mir::secret_leak

  × a `secret` value reaches a conditional branch, which would leak it via
  │ timing
    ╭─[tests/ui/c65_secret_join_guarded.sentinel:32:5]
 31 │     let x: secret bool = if c { return t } else { bs[if c { 0 } else { return t }] };
 32 │     x && t
    ·     ┬
    ·     ╰── secret value reaches the sink here
 33 │ }
    ╰────
  help: a secret must not influence control flow, a memory address, or a
        variable-latency instruction; restructure to be branch-free (e.g. a
        masked select) or `declassify` the value first if the leak is
        acceptable

sentinel::mir::secret_leak

  × a `secret` value reaches a conditional branch, which would leak it via
  │ timing
    ╭─[tests/ui/c65_secret_join_guarded.sentinel:52:20]
 51 │         let x: secret bool = if c { return false } else { bs[0] };
 52 │         declassify(x && t)
    ·                    ┬
    ·                    ╰── secret value reaches the sink here
 53 │     } else {
    ╰────
  help: a secret must not influence control flow, a memory address, or a
        variable-latency instruction; restructure to be branch-free (e.g. a
        masked select) or `declassify` the value first if the leak is
        acceptable

sentinel::mir::secret_leak

  × a `secret` value reaches a conditional branch, which would leak it via
  │ timing
    ╭─[tests/ui/c65_secret_join_guarded.sentinel:60:16]
 59 │     let x: secret bool = if c { return false } else { while c2 { return false; } bs[0] };
 60 │     declassify(x && t)
    ·                ┬
    ·                ╰── secret value reaches the sink here
 61 │ }
    ╰────
  help: a secret must not influence control flow, a memory address, or a
        variable-latency instruction; restructure to be branch-free (e.g. a
        masked select) or `declassify` the value first if the leak is
        acceptable

sentinel::mir::secret_leak

  × a `secret` value reaches a conditional branch, which would leak it via
  │ timing
    ╭─[tests/ui/c65_secret_join_guarded.sentinel:65:16]
 64 │     let x: secret bool = if c { return false } else { match mk(n) { E::A => bs[0], E::B(v) => match mk(m) { E::A => return false, E::B(w) => return true } } };
 65 │     declassify(x && t)
    ·                ┬
    ·                ╰── secret value reaches the sink here
 66 │ }
    ╰────
  help: a secret must not influence control flow, a memory address, or a
        variable-latency instruction; restructure to be branch-free (e.g. a
        masked select) or `declassify` the value first if the leak is
        acceptable

sentinel::mir::secret_leak

  × a `secret` value reaches a conditional branch, which would leak it via
  │ timing
    ╭─[tests/ui/c65_secret_join_guarded.sentinel:73:24]
 72 │             let q = match mk(m) { E::A => t, E::B(w) => t };
 73 │             declassify(q && t)
    ·                        ┬
    ·                        ╰── secret value reaches the sink here
 74 │         }
    ╰────
  help: a secret must not influence control flow, a memory address, or a
        variable-latency instruction; restructure to be branch-free (e.g. a
        masked select) or `declassify` the value first if the leak is
        acceptable

sentinel::mir::secret_leak

  × a `secret` value reaches a conditional branch, which would leak it via
  │ timing
    ╭─[tests/ui/c65_secret_join_guarded.sentinel:80:16]
 79 │     let x = match mk(n) { E::A => { let q: bool = match mk(m) { E::A => bs[0], E::B(w) => false }; t }, E::B(v) => t };
 80 │     declassify(x && t)
    ·                ┬
    ·                ╰── secret value reaches the sink here
 81 │ }
    ╰────
  help: a secret must not influence control flow, a memory address, or a
        variable-latency instruction; restructure to be branch-free (e.g. a
        masked select) or `declassify` the value first if the leak is
        acceptable
