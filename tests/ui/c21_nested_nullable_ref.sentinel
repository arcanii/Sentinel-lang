// `&mut ?&T` is a nested reference: a callee could store `&local` into the caller's slot.
fn stash(slot: &mut ?&i64) -> i64 { let x: i64 = 5; *slot = &x; 0 }
fn main() -> i64 { let z: i64 = 1; let mut h: ?&i64 = &z; let k: i64 = stash(&mut h); *unwrap_or(h, &z) }
