// C3.4 / ADR 0020 D5: handler arm references an op not declared on
// the named effect. Surfaces
// `sentinel::resolve::undefined_handler_op` at resolve.

effect Io {
    read() -> i64;
}

fn main() -> i64 {
    handle 0 with { Io.write(k) => k(1) }
}
