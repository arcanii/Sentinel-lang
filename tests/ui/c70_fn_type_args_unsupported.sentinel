// ADR 0070 D1 (generalized): `Fn<T, R>` requires BOTH T and R to be
// word-scalars (i64/i32/u8/bool/f64/ptr) — `u128` doesn't fit the plain
// LLVM function-pointer signature (no encode/decode step, unlike
// `Channel<T>`/`process_send`, so the param/return types must already be
// ordinary LLVM scalar types). Surfaces FnTypeArgsNotSupported.
fn apply_to(op: Fn<u128, i64>, x: u128) -> i64 {
    0
}

fn main() -> i64 {
    0
}
