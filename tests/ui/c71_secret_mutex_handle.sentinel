// ADR 0071 D2 amendment A1: a `Mutex` handle cannot be `secret`-qualified either — the
// secret belongs inside the container, `Mutex<secret T>` (ADR 0071 D6). Surfaces
// SecretHandle at the parameter's annotation.

fn hold(m: secret Mutex<i64>) -> i64 {
    1
}

fn main() -> i64 {
    0
}
