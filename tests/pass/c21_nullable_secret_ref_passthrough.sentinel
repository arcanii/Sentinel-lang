// `?&T` and `secret &T` are ref-carrying: caller-owned ones pass straight through.
fn f(x: ?&i64) -> ?&i64 { x }
fn g(x: secret &i64) -> secret &i64 { x }
fn main() -> i64 { let z: i64 = 3; let a: ?&i64 = f(&z); let s: secret &i64 = g(&z); let p: &i64 = declassify(s); *unwrap_or(a, &z) + *p }
