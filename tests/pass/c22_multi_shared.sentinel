// C2.2 / ADR 0017 D6: multiple shared `&T` borrows of the same
// place coexist (shared-XOR-mutable allows multiple shareds).
// 5 + 5 + 5 = 15 -> exit 15.
fn main() -> i64 {
    let x: i64 = 5;
    let r1: &i64 = &x;
    let r2: &i64 = &x;
    let r3: &i64 = &x;
    *r1 + *r2 + *r3
}
